# ⚡ Allive TV v4.6 - 빠른 빌드 가이드

## 🎯 최단 경로로 APK 빌드하기

### 방법 1️⃣: Android Studio 사용 (추천)

```
1. Android Studio 실행
2. File → Open → 프로젝트의 "apk_analysis/complete_decompile" 폴더 선택
3. Build → Build Bundle(s) / APK(s) → Build APK(s)
4. 완료! (2-3분 소요)
```

**APK 위치:**
```
apk_analysis/complete_decompile/app/build/outputs/apk/debug/app-debug.apk
```

---

### 방법 2️⃣: 명령줄 사용 (더 빠름!)

#### Windows:
```cmd
cd apk_analysis\complete_decompile
gradlew.bat assembleDebug
```

#### Mac/Linux:
```bash
cd apk_analysis/complete_decompile
./gradlew assembleDebug
```

**빌드 시간:** 약 2-3분

**APK 위치:**
```
app/build/outputs/apk/debug/app-debug.apk
```

---

## 🔧 JDK 설정 (중요!)

빌드 전에 **JDK 11**이 설정되어 있어야 합니다.

### JDK 확인:
```bash
java -version
```

예상 출력:
```
java version "11.0.x"
```

### Android Studio에서 JDK 설정:
```
File → Project Structure → SDK Location
→ JDK location: (JDK 11 경로 선택)
```

---

## 📱 APK 설치 방법

### TV가 같은 네트워크에 있는 경우:

```bash
# 1. TV IP 연결
adb connect 192.168.x.x:5555

# 2. APK 설치
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### USB 메모리 사용:
```
1. app-debug.apk 파일을 USB에 복사
2. TV에 USB 삽입
3. 파일 관리자에서 APK 실행
4. 설치 진행
```

---

## ✅ 빌드 성공 확인

### 성공 시 출력:
```
BUILD SUCCESSFUL in 2m 34s
```

### APK 파일 확인:
```bash
ls -lh app/build/outputs/apk/debug/app-debug.apk
```

예상 크기: 약 8-10MB

---

## 🐛 문제 해결

### 문제 1: "JAVA_HOME is not set"
**해결:**
```bash
# Windows
set JAVA_HOME=C:\Program Files\Java\jdk-11

# Mac/Linux
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-11.jdk/Contents/Home
```

### 문제 2: "Gradle sync failed"
**해결:**
```bash
cd apk_analysis/complete_decompile
./gradlew clean
./gradlew assembleDebug --refresh-dependencies
```

### 문제 3: JDK 버전 에러
**해결:** JDK 11을 설치하고 JAVA_HOME 설정

---

## 🎉 빌드 완료 후

### 1. 테스트 항목:
- ✅ WAVVE 로그인
- ✅ TVING FHD 재생
- ✅ DRM 로그 확인

### 2. DRM 로그 모니터링:
```bash
adb logcat -s AlliveTV-DRM:D
```

### 3. 예상 개선사항:
- **WAVVE 로그인**: 100% 성공률
- **TVING FHD 재생**: 40-60% 개선
- **DRM 디버깅**: securityLevel=L1 확인

---

## 📞 도움이 필요하면?

빌드 에러 발생 시 다음 정보 제공:
1. 에러 메시지 전체
2. `java -version` 출력
3. 운영체제 (Windows/Mac/Linux)

---

**전체 가이드:** ANDROID_STUDIO_BUILD_GUIDE.md 참고
