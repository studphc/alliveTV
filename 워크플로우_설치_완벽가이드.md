# ⚡ GitHub Actions 워크플로우 설치 (1분 완료!)

## 📍 현재 상황
✅ 저장소 Actions 권한: **완벽 설정됨**
✅ 워크플로우 파일: **이미 준비됨** (`build-apk.yml`)
⚠️ 문제: GitHub App은 `.github/workflows/` 푸시 불가 (보안 정책)

---

## 🎯 **가장 쉬운 방법 (GitHub 웹 UI - 1분)**

### **옵션 1: 파일 업로드 방식 (초간단!)**

1. **워크플로우 파일 다운로드**
   ```
   https://github.com/studphc/alliveTV/raw/revival-project/build-apk.yml
   ```
   - 우클릭 → "다른 이름으로 링크 저장" → `build-apk.yml` 저장

2. **GitHub에서 폴더 생성 & 업로드**
   - 저장소로 이동: https://github.com/studphc/alliveTV
   - `revival-project` 브랜치 선택
   - `Add file` → `Upload files` 클릭
   - 파일명을 `.github/workflows/build-apk.yml`로 변경
   - "Commit changes" 클릭

---

### **옵션 2: 복사-붙여넣기 방식**

1. **파일 내용 보기**
   ```
   https://github.com/studphc/alliveTV/blob/revival-project/build-apk.yml
   ```
   - "Raw" 버튼 클릭
   - 전체 선택 (Ctrl+A) → 복사 (Ctrl+C)

2. **새 파일 만들기**
   ```
   https://github.com/studphc/alliveTV/new/revival-project
   ```
   - 파일명: `.github/workflows/build-apk.yml`
   - 복사한 내용 붙여넣기 (Ctrl+V)
   - "Commit new file" 클릭

---

### **옵션 3: GitHub CLI (터미널이 익숙하다면)**

```bash
# 1. 저장소 클론 (없는 경우)
git clone https://github.com/studphc/alliveTV.git
cd alliveTV

# 2. 브랜치 전환
git checkout revival-project

# 3. 파일 이동
mkdir -p .github/workflows
mv build-apk.yml .github/workflows/

# 4. 커밋 & 푸시
git add .github/workflows/build-apk.yml
git commit -m "feat: Enable GitHub Actions workflow"
git push origin revival-project
```

---

## ✅ **설치 완료 확인**

워크플로우 추가 후:

1. **Actions 페이지 확인**
   ```
   https://github.com/studphc/alliveTV/actions
   ```
   - `Build Allive TV APK` 워크플로우 표시됨

2. **수동 빌드 실행 (선택)**
   - 워크플로우 선택 → `Run workflow` 클릭
   - 브랜치: `revival-project` 선택
   - `Run workflow` 버튼 클릭

3. **자동 빌드 (자동)**
   - 다음 `push`부터 자동으로 빌드 시작

---

## 📥 **APK 다운로드**

빌드 완료 후 (5-10분):

**방법 1: Actions 아티팩트**
```
https://github.com/studphc/alliveTV/actions
→ 완료된 워크플로우 클릭
→ "Artifacts" 섹션
→ "allive-v4.6-revival" 다운로드
```

**방법 2: Release 페이지**
```
https://github.com/studphc/alliveTV/releases
→ 최신 v4.6-revival-* 선택
→ "allive-v4.6-revival.apk" 다운로드
```

---

## 🎯 **요약**

| 단계 | 소요 시간 |
|------|----------|
| 1. 워크플로우 파일 복사 | 10초 |
| 2. GitHub에 업로드/생성 | 30초 |
| 3. 커밋 | 10초 |
| 4. 빌드 대기 | 5-10분 |
| **총 소요 시간** | **~1분 + 빌드** |

---

## 💡 **왜 GenSpark가 직접 못 하나요?**

GitHub의 보안 정책:
- **일반 파일**: GitHub App이 자유롭게 푸시 가능 ✅
- **워크플로우 파일** (`.github/workflows/*.yml`): 
  - 특별 권한 필요 (`workflows` scope)
  - 보안상 사용자가 직접 추가해야 함 🔒

---

## 🎉 **준비 완료!**

위 방법 중 편한 걸 선택하시면 됩니다!

**가장 추천하는 방법은 "옵션 2: 복사-붙여넣기"입니다!**
