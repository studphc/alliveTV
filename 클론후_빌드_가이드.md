# 🎯 GitHub 클론 후 빌드 가이드

## 현재 상황
✅ GitHub에서 프로젝트 클론 완료  
✅ Android Studio 실행됨  
❓ 다음 단계는?

---

## 🚀 Step-by-Step 가이드

### 1️⃣ **프로젝트 열기 (가장 중요!)**

현재 Android Studio 화면에서:

```
File → Open
```

또는 시작 화면에서:

```
Open an Existing Project
```

### 2️⃣ **올바른 폴더 선택**

⚠️ **중요**: 루트 폴더가 아닌 **빌드 가능한 하위 폴더**를 열어야 합니다!

#### ❌ 잘못된 경로:
```
alliveTV/                          ← 이게 아님!
```

#### ✅ 올바른 경로:
```
alliveTV/apk_analysis/complete_decompile/    ← 이 폴더 선택!
```

**찾는 방법**:
1. 클론한 `alliveTV` 폴더로 이동
2. `apk_analysis` 폴더 열기
3. `complete_decompile` 폴더 선택
4. **OK** 클릭

### 3️⃣ **Gradle 동기화 대기**

프로젝트를 열면 Android Studio가 자동으로:
- Gradle 파일을 읽음
- 의존성을 다운로드
- 프로젝트를 설정

하단에 다음 메시지가 나타날 때까지 대기:
```
✓ Gradle sync finished in Xs XXXms
```

⏱️ **소요 시간**: 첫 실행 시 1-3분

### 4️⃣ **JDK 설정 확인** (중요!)

프로젝트를 연 후:

```
File → Project Structure (또는 Ctrl+Alt+Shift+S)
```

왼쪽 메뉴에서:
```
SDK Location
```

확인 사항:
- **JDK location**: JDK 11이 설정되어 있는지 확인
- 없다면: Download JDK... → 11 선택

### 5️⃣ **빌드 시작!**

상단 메뉴:
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

또는 단축키:
- **Mac**: `Cmd + Shift + F9`
- **Windows/Linux**: `Ctrl + Shift + F9`

### 6️⃣ **APK 찾기**

빌드 완료 후 우측 하단 알림:
```
APK(s) generated successfully
┗━ Locate
```

**Locate** 클릭하면 APK 파일 위치로 이동!

또는 수동으로:
```
alliveTV/apk_analysis/complete_decompile/app/build/outputs/apk/debug/app-debug.apk
```

---

## 🖥️ Android Studio UI 가이드

### 프로젝트 구조 (왼쪽 패널):
```
complete_decompile/
├── app/                      ← 메인 앱
│   ├── src/
│   │   └── main/
│   │       ├── java/         ← 소스 코드 (WAVVE, TVING 수정됨)
│   │       ├── res/          ← 리소스
│   │       └── AndroidManifest.xml
│   └── build.gradle          ← 앱 빌드 설정
├── build.gradle              ← 프로젝트 빌드 설정
└── settings.gradle
```

### 주요 메뉴:
```
🔨 Build → Build APK(s)           빌드 시작
🔄 File → Sync Project with Gradle Files    동기화
🔧 File → Project Structure        설정
🏃 Run → Run 'app'                에뮬레이터 실행 (선택사항)
```

---

## 🐛 문제 해결

### 문제 1: "Project SDK is not defined"
**증상**: 프로젝트를 열었는데 SDK 에러

**해결**:
```
File → Project Structure → Project
→ SDK: (Android API 30 또는 33 선택)
→ Apply
```

### 문제 2: "Gradle sync failed"
**증상**: 동기화 실패 메시지

**해결 1**: JDK 버전 확인
```
File → Settings → Build Tools → Gradle
→ Gradle JVM: 11 선택
→ Apply
```

**해결 2**: 인터넷 연결 확인 후:
```
File → Invalidate Caches / Restart
→ Invalidate and Restart
```

### 문제 3: "Cannot resolve symbol"
**증상**: 코드에 빨간 밑줄

**해결**:
```
File → Sync Project with Gradle Files
```

또는:
```
Build → Clean Project
Build → Rebuild Project
```

### 문제 4: 잘못된 폴더를 열었을 때
**증상**: Gradle 파일을 찾을 수 없다는 에러

**해결**:
1. File → Close Project
2. 다시 Open
3. `apk_analysis/complete_decompile/` 폴더 선택 (루트 아님!)

---

## ✅ 체크리스트

프로젝트를 올바르게 열었는지 확인:

- [ ] 왼쪽 패널에 `app` 폴더가 보임
- [ ] `build.gradle` 파일이 보임
- [ ] 하단에 "Gradle" 탭이 있음
- [ ] 상단에 초록색 망치(🔨) 아이콘이 있음
- [ ] JDK 11이 설정됨

모두 체크되면 빌드 준비 완료! ✅

---

## 📊 예상 시간

| 단계 | 소요 시간 |
|------|----------|
| 프로젝트 열기 | 10초 |
| Gradle 동기화 (첫 실행) | 1-3분 |
| APK 빌드 | 2-3분 |
| **총 시간** | **약 5분** |

---

## 🎬 전체 프로세스 요약

```
1. Android Studio 실행 ✅
2. File → Open ✅
3. alliveTV/apk_analysis/complete_decompile/ 선택 ⬅️ 지금 여기
4. Gradle 동기화 대기 (자동)
5. Build → Build APK(s)
6. APK 완성! 🎉
```

---

## 📱 빌드 후 설치

APK가 완성되면:

```bash
# TV 연결
adb connect 192.168.x.x:5555

# APK 설치
adb install -r app-debug.apk
```

---

## 💡 팁

### 빠른 빌드 (명령줄):
Android Studio 없이도 빌드 가능:

```bash
# 클론한 폴더로 이동
cd alliveTV/apk_analysis/complete_decompile

# Windows
gradlew.bat assembleDebug

# Mac/Linux
./gradlew assembleDebug
```

### 빌드 변형:
- **Debug APK**: 테스트용, 빠름
  ```
  Build → Build APK(s)
  ```
  
- **Release APK**: 배포용, 서명 필요
  ```
  Build → Generate Signed Bundle / APK
  ```

---

## 🔗 다음 단계

APK 빌드 성공 후:

1. **TV에 설치**
2. **WAVVE 로그인 테스트**
3. **TVING FHD 재생 테스트**
4. **DRM 로그 확인**:
   ```bash
   adb logcat -s AlliveTV-DRM:D
   ```

---

## 📞 추가 도움

문제가 계속되면:
1. Android Studio 하단 "Build" 탭의 에러 메시지 복사
2. 스크린샷 찍기
3. 질문하기!

**지금 해야 할 일**: `apk_analysis/complete_decompile` 폴더를 Android Studio에서 열기!
